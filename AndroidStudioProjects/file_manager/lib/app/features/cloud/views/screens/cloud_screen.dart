import 'package:flutter/material.dart';

class CloudSreen extends StatelessWidget {
  const CloudSreen({Key? key}) : super(key: key);

  /// God's Great everytime
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("cloud"),
      ),
    );  }
}
