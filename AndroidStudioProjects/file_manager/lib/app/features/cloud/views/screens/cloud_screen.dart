import 'package:flutter/cupertino.dart';

class CloudSreen extends StatelessWidget {
  const CloudSreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
