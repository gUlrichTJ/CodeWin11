#include <stdio.h>
#include <stdlib.h>
#include "tab_in.h"

int main()
{
    int A[100][100], B[100][100], C[100][100];
    int N, M, P;
    int i = 0, j = 0, k = 0, verification = 0;    // Indices
    //system("clear");
    /// The size of the table A
    N = lignesMatrice();
    M = colonnesMatrice();

    return 0;
}
